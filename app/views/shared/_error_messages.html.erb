<% if @bookreceipt %>
  <% if @bookreceipt.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(@bookreceipt.errors.count, "error") %> 
          prohibited this user from being saved:</h3>
      <p>There were problems with the following fields:</p>
      <ul>
      <% @bookreceipt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% end %>

